#!/usr/bin/env php
<?php
declare(strict_types=1);

define('ATOMIC_START', microtime(true));
define('ATOMIC_ROOT', __DIR__);

$app = require ATOMIC_ROOT . '/bootstrap/app.php';

exit($app->handle_command($argv));
